<UserControl x:Class="Enrollment.Scanner.Components.DeviceSelector.DeviceSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Enrollment.Scanner.Components.DeviceSelector"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:DeviceSelectorViewModel, IsDesignTimeCreatable=false}"
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded" x:ClassModifier="internal">
    <Grid>
        <ListBox x:Name="ScannerList">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>fwewe</TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
